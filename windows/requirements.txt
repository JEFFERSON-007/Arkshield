# ArkShield Windows Desktop Application Requirements

# Backend framework (REQUIRED)
fastapi>=0.111.0
uvicorn>=0.29.0
pydantic>=2.0.0

# System monitoring (REQUIRED)
psutil>=5.9.0

# Desktop application framework (OPTIONAL - graceful fallback to Edge app mode)
# pywebview>=4.0.0  # Commented out due to pythonnet build issues on Python 3.14+

# Build tools (for creating .exe)
pyinstaller>=6.0.0

# Optional: For better Windows integration
pillow>=10.0.0  # For icon handling
